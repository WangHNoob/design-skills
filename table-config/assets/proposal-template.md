# 配表变更提案：{表名}

> 生成时间：{日期} ｜ 目标表：`{表名}` ｜ 模式：{append 新增 / replace 替换}
> 上游权威表在 SVN，本提案供策划审核后手动应用（本 skill 只读知识库、不改原始表）。

## 一、变更概述
- 目标表：`{表名}`（{中文用途}）
- 变更类型：新增 {N} 行 / 修改 {M} 行
- 来源依据：{对应的数值设计文档 / 需求出处}

## 二、变更数据
（完整数据见同目录 `{表名}.rows.tsv`，可直接粘贴；预览见 `{表名}.preview.xlsx`）

| {字段1} | {字段2} | ... | 依据/说明 |
|---|---|---|---|
| {值} | {值} | ... | {这行为什么这么填} |

**关键值依据**：
- `{字段=值}`：{来源，如"依据 numeric-累充.docx 第3档定价" / "同表已有档位线性外推"}

## 三、校验结果
- **主键唯一**：{通过 / 冲突项}（table_tool.py validate）
- **字段/类型**：{齐全、类型匹配 / 问题}
- **外键存在性**（kb_check_table_value）：
  - `{道具ID}` 在 `{道具表}`：{存在 ✓ / ✗ 待确认}
- **取值范围**：{在同表既有范围内 / 异常值及依据}

## 四、如何应用（策划操作）
1. 打开 SVN 工作副本里的 `{表名}.xlsx`。
2. 选中数据区末尾（或对应行），把 `{表名}.rows.tsv` 的内容按 `{列序}` 粘入。
3. 核对预览 `{表名}.preview.xlsx` 无误。
4. `svn commit -m "{表名}: {变更说明}"`，走正常构建/发布流程。

## 五、待确认 / 风险
- {未能验证的引用、需人工拍板的值、可能的连锁影响}
