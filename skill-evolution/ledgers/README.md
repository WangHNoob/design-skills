# 经验账本目录

每个被管理的策划 skill 在这里有一份账本：`<skill>.md`（如 `combat-design.md`）。
账本在首次为某 skill 捕获经验时按 `../assets/learnings-template.md` 创建。

- 反思阶段：向对应账本「待固化」区追加条目。
- 固化阶段：把已固化条目标记状态并记入 `../CHANGELOG.md`。

本目录不参与 `.claude` / `.agents` 镜像同步，只保留一份。
